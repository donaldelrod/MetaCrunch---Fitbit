import Influx from 'influx'

export default class InfluxInterface {

    influx = null;
    user = 'influx';
    pass = 'influx';
    host = 'localhost';

    constructor(user, pass, host) {
        
    }
}